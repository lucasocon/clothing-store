<div class="container">
  <%= form_tag root_path, :method => 'get' do %>
    <p>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Search", :name => nil %>
    </p>
  <% end %>
  <% @products.each do |p| %>
    <p>
      <b>
        <%= p.brand.name %>
      </b>
      <i>
        <%= p.clothing_type.name %>
      </i>
    </p>
  <% end %>
</div>
